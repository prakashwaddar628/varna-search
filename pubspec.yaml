name: varna_search
description: "Varna-Search Pro — A commercial desktop application for high-volume design studios."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI Framework (Modern OS-native look)
  fluent_ui: ^4.11.1
  
  # State Management
  flutter_riverpod: ^2.5.1
  
  # Database (Vector/Metadata lookup)
  objectbox: ^4.0.0
  objectbox_flutter_libs: any
  
  # AI Inference
  # onnxruntime_flutter: ^0.1.0
  
  # Backend / Monetization Validation
  supabase_flutter: ^2.5.0
  
  # Security / Storage
  path_provider: ^2.1.2
  url_launcher: ^6.2.5
  
  # Utils
  uuid: ^4.4.0
  dartz: ^0.10.1 # For functional programming/Either
  logger: ^2.3.0
  shared_preferences: ^2.5.4
  document_file_save_plus: ^2.0.0
  desktop_drop: ^0.7.0
  file_selector: ^1.1.0
  image: ^4.8.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.9
  objectbox_generator: any

flutter:
  uses-material-design: true
  assets:
    - assets/models/clip_model.onnx
    - assets/images/
